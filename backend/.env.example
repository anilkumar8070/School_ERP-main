# Database connection string (PostgreSQL)
# Note: Add ?connection_limit=10 to the URL to optimize connection pooling when running multiple Node.js workers
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/school_erp?schema=public&connection_limit=10"
JWT_SECRET=supersecretjwtkey12345
PORT=4000
